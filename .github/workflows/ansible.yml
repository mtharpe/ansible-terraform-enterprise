name: Ansible lint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repo
      - uses: actions/checkout@v2
      - name: ansible-lint
        uses: ansible/ansible-lint-action@master
        with:
          targets: ""
